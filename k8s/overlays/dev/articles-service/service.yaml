apiVersion: v1
kind: Service
metadata:
  name: dev-articles-service
  namespace: scrollfeed-dev
  labels:
    app: dev-articles-service
    environment: dev
    team: platform
spec:
  selector:
    app: dev-articles-service
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
