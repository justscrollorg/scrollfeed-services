apiVersion: v1
kind: Service
metadata:
  name: dev-jokes-service
  namespace: scrollfeed-dev
  labels:
    app: dev-jokes-service
    environment: dev
    team: platform
spec:
  selector:
    app: dev-jokes-service
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
