apiVersion: v1
kind: Service
metadata:
  name: dev-video-service
  namespace: scrollfeed-dev
  labels:
    app: dev-video-service
    environment: dev
    team: platform
spec:
  selector:
    app: dev-video-service
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
  type: ClusterIP
