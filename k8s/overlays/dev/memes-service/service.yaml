apiVersion: v1
kind: Service
metadata:
  name: dev-memes-service
  namespace: scrollfeed-dev
  labels:
    app: dev-memes-service
    environment: dev
    team: platform
spec:
  selector:
    app: dev-memes-service
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
  type: ClusterIP
